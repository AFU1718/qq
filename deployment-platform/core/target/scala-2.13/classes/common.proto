syntax = "proto3";

package com.zz.ady.idl.backstage;

import "scalapb/scalapb.proto";

option (scalapb.options) = {
  preserve_unknown_fields: false
};

option optimize_for = SPEED;

// 下拉列表返回
message SelectResult {
    int64                       id                          = 1;            // ID
    string                      name                        = 2;            // 名称
}
